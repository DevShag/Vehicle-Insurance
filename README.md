# Vehicle-Insurance
This project covers the complete lifecycle of a machine learning system, including: Data ingestion, Data validation, Data transformation, Model training, Model evaluation, Model deployment. The system is designed with a modular architecture so the same template can be reused for multiple ML projects.
